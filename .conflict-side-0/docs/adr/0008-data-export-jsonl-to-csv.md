# 数据导出系统：JSONL→CSV 转换 + 配置导出

审计日志导出：读取 JSONL 文件，转为 CSV（Excel 可打开）。已处理文件记录导出：读取 NDJSON，转为 CSV。配置导出：直接复制 config.json。导出入口：UI 按钮或 CLI 命令。约 60 行代码。

## Considered Options

- **仅 JSONL 原样导出**：零转换代码，但用户需要自己解析 JSON。被否决——对普通用户不友好。
- **不做导出**：用户直接去目录复制文件。被否决——合规需求（向第三方证明元数据已清理）。
