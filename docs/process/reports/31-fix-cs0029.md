# 报告 — 票 31 返修：CS0029 调用点类型不匹配（架构恢复第七轮）

- 日期：2026-09-06　窗口：票 31 返修窗　分支：arc-recovery/31-polling-hosted-service（GitButler 虚拟分支，31 叠 30 叠 29，commit 以 git log 实物为准）　未 push 不 PR。
- 启动器：prompts/31-fix-cs0029.md；必读清单已逐份读全（review-31 / handoffs-31 / issues-31 / spec / WORKFLOW / MainWindow.axaml.cs / WindowPollingHostedService.cs / WindowPollingHostedServiceSourceTests.cs）。

## 检查点 A — review-31 逐条实物复核

| review-31 结论 | 实物复核 | 结论 |
|---|---|---|
| run 34035351753 CS0029 @ MainWindow.axaml.cs:248:49 `Cannot implicitly convert type 'System.Threading.Tasks.Task' to 'string'` | gh run view 34035351753 --log-failed 实物：`##[error]…(248,49): error CS0029: Cannot implicitly convert type 'System.Threading.Tasks.Task' to 'string' [PhotoPrivacy.Ui.csproj]`；Core.Tests 191/191 绿、IntegrationTests 被 Ui 编译错阻断 | ✅ 复现确认 |
| 机制推测「旧方法签名返回 string，形/实参类型不兼容」 | **修正**：BackgroundUiOptions.cs:27 实类型 `Func<CancellationToken, Task<string>>` 与新方法形参完全一致，形/实参**无不匹配**；旧方法本就是 async Task + `_ =` discard。真根因 = 票 31 重写调用点时把 lambda 参数名 `exePath` 改成 `_`——`onExifToolExePathDetected` 委托是 `Action<string?>`，`_` 被参数绑定后不再是 discard，体内 `_ = Task` 变成 Task→string 参数赋值 → CS0029。262 行同类调用在普通语句作用域（`_` 无绑定）保持 discard 所以 CI 只报 248 一处，与本机制完全吻合（列 49 正指向赋值表达式） | ✅ 根因实锤（方向同 review，机制修正） |
| git grep 调用点类型 | 3 处 src 调用点：248（病灶）/ 262（合法 discard）/ 990（注释）；守卫 T2 断言不含 lambda 参数名，不受影响 | ✅ |
| but status 分支落位（31 叠 30 叠 29） | ar [31] → rc [30] → re [29]，zz clean | ✅ |
| 过程违规：窗口侧 0 项；口径噪声（1056 vs 1057 / 174 vs 175 / 4 文件未计 / issue 未勾选） | wc -l（换行符计数）与 Split('\n')（端值差）口径差所致，行数锁 ≤1100 大幅宽松不受影响；issue 状态为大脑领地（WORKFLOW §4.3）窗口不动，交大脑勾选 | ✅ 如实记录 |

## 检查点 B — 修复（最小改动）

MainWindow.axaml.cs:248 一行：`onExifToolExePathDetected: _ =>` → `onExifToolExePathDetected: exePath =>`（参数名恢复旧代码形态，`_ =` 回归 discard 语义；与票 31 前旧代码 `exePath => _ = ApplyExifToolVersionFromIpcAsync(),` 同构，仅调用目标换宿主服务）。git diff --stat：1 文件 1 insertion / 1 deletion。**循环体、取消语义、Dispatcher 投递、StartAsync 延迟 Activate 时序、行数（1057）与守卫口径全部不动。**

防再犯说明：该错误为编译期错误（CS0029 CI 必拦），非守卫可静态拦截的语义回潮；启动器明确「守卫口径不动」，未新增守卫。

## 检查点 C — 对照完成定义（handoff 3 条 + issue 4 checkbox）

| # | 声明 | 证据 | 结论 |
|---|---|---|---|
| 1 | 两轮询脱离 MainWindow 下沉宿主服务，退避/取消语义保持 | review-31 #1/#3 已确认（✅）；本返修零触碰（diff 仅 248 行参数名） | ✅ |
| 2 | MainWindow 行数下降 | a10f2b6=1158 → 现工作区 1057（Split 口径；wc -l 口径 1056），本返修不改行数 | ✅ |
| 3 | CI 验证分支守卫测试全绿（**唯一验收**） | 本机 CI-only 零 build/test；静态门禁全绿：守卫 T1–T6 全量重演 PASS + CS0029 病灶专项（`_ =>` 形态清零 / exePath discard 恢复 / 262 discard 完好）+ git diff --check CLEAN + 括号平衡/LF/BOM 基线不动。**云端绿待大脑推送验证分支确认** | ✅（静态口径；云端待大脑） |
| 4 | 报告双轨随 commit | 本文件 + docs/process/reports/31-fix-cs0029.md 副本随返修 commit | ✅ |

## 检查点 D — 交接

- 本窗口零推送、零本地构建/测试（CI-only 政策）。
- 交大脑：推送验证分支触发 workflow（预期 CS0029 消除、Core 191 + Integration 全绿）；issue 31 checkbox 勾选与状态流转（§4.3 大脑领地）。
- 若 CI 仍红：下一可疑点为 (172,33) CS8602 nullable 警告（warning 非 error，不阻断）或后续编译错误，按 .scratch 流程重开返修窗。
